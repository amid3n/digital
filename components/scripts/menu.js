jQuery(function ($) {
    if ($(window).width() < 1024)

    let menu = $('.menu');

    $(window).resize(function () {
        // let screenWidth = $(window).width();
        //
        // if (screenWidth < 1024) {
        //     menu.eq(0).append(menu.eq(1));
        // }
    })
});